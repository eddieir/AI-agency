import type{Metadata}from"next";import Link from"next/link";import{SiteHeader}from"@/components/SiteHeader";import{SiteFooter}from"@/components/SiteFooter";import{PageHero}from"@/components/PageHero";import{tools}from"@/lib/catalog";
export const metadata:Metadata={title:"Compare AI Tools — Free vs Paid",description:"Compare developer and creative AI tools by audience, category, pricing, and use.",alternates:{canonical:"/compare/tools"}};export default function Page(){return <><SiteHeader/><main><PageHero code="UTILITY / TOOL COMPARE" title="Compare the" italic="working stack." description="Developer and creative tools organized by audience, category, price, and strongest use." tone="dark"/><div className="compare-subnav"><Link href="/compare">MODEL COMPARATOR</Link><Link className="active" href="/compare/tools">TOOL COMPARATOR</Link></div><section className="content-shell comparison-shell"><div className="compare-table"><div className="compare-row compare-head"><span>TOOL</span><span>FOR</span><span>CATEGORY</span><span>PRICE</span><span>BEST FOR</span><span/></div>{tools.map(t=><div className="compare-row" key={t.slug}><span className="compare-name"><i>{t.mark}</i><b>{t.name}</b><small>{t.company}</small></span><span>{t.audience}</span><span>{t.category}</span><span><em className={`pill pill-${t.price.toLowerCase().replace(" ","-")}`}>{t.price}</em></span><span>{t.bestFor.slice(0,2).join(" · ")}</span><a href={t.url} target="_blank" rel="noreferrer">↗</a></div>)}</div></section></main><SiteFooter/></>}
