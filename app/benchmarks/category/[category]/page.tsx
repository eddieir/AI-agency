import type{Metadata}from"next";import{notFound}from"next/navigation";import{SiteHeader}from"@/components/SiteHeader";import{SiteFooter}from"@/components/SiteFooter";import{BenchmarkExplorer}from"@/components/BenchmarkExplorer";import{benchmarkCategories,metricInfo,type MetricKey}from"@/lib/benchmarks";
export function generateStaticParams(){return benchmarkCategories.map(category=>({category}))}export async function generateMetadata({params}:{params:Promise<{category:string}>}):Promise<Metadata>{const{category}=await params,info=metricInfo[category as MetricKey];return info?{title:`${info.name} AI Model Leaderboard`,description:info.description,alternates:{canonical:`/benchmarks/category/${category}`},openGraph:{images:[]},twitter:{images:[]}}:{}}
export default async function Page({params}:{params:Promise<{category:string}>}){const{category}=await params;if(!benchmarkCategories.includes(category as MetricKey))notFound();const key=category as MetricKey,info=metricInfo[key];return <><SiteHeader/><main className="bench-main"><section className="bench-category-hero"><span>BENCHMARK BOARD / {info.short}</span><h1>{info.name}</h1><p>{info.description}</p><div><b>{info.method}</b></div></section><section className="bench-shell category-board"><BenchmarkExplorer initialMetric={key}/><div className="source-panel"><div><span>PRIMARY METHODOLOGY</span><h2>Read the evaluation before the rank.</h2><p>Scores on this board are not interchangeable with scores from another test.</p></div><a href={info.source} target="_blank" rel="noopener noreferrer">OPEN ORIGINAL METHODOLOGY ↗</a></div></section></main><SiteFooter/></>}
