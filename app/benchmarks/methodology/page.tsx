import type{Metadata}from"next";import Link from"next/link";import{SiteHeader}from"@/components/SiteHeader";import{SiteFooter}from"@/components/SiteFooter";import{metricInfo}from"@/lib/benchmarks";
export const metadata:Metadata={title:"AI Benchmark Methodology and Evidence Policy",description:"How AXON//RADAR sources, labels, versions, and limits AI model benchmark evidence.",alternates:{canonical:"/benchmarks/methodology"}};const rules=["A model ranks only within a named evaluation; unrelated tests are never averaged.","Independent measurements and provider claims must carry explicit labels.","Unpublished results remain blank—we never interpolate or estimate scores.","Benchmark version, reasoning effort, harness, API and date remain attached.","Small score differences may not be practically meaningful; test your own workload.","Source corrections trigger a reviewed snapshot update and new evidence date."];
export default function Page(){return <><SiteHeader/><main className="seo-detail guide-detail"><div className="seo-breadcrumb"><Link href="/benchmarks">Benchmarks</Link><span>/</span><span>Methodology</span></div><article><header><span>BENCHMARK GOVERNANCE</span><time>Reviewed August 20, 2026</time><h1>Evidence before ranking.</h1><p>The rules used to make model comparisons useful without pretending they are universal truth.</p></header>{rules.map((body,i)=><section key={body}><small>0{i+1}</small><h2>{["Benchmark-specific ranks","Independent versus reported","Missing means missing","Versions remain attached","No false precision","Correction policy"][i]}</h2><p>{body}</p></section>)}<aside><h2>Primary methods</h2>{Object.values(metricInfo).map(m=><a href={m.source} target="_blank" rel="noopener noreferrer" key={m.name}>{m.name}</a>)}</aside></article></main><SiteFooter/></>}
