import type{Metadata}from"next";import Link from"next/link";import{SiteHeader}from"@/components/SiteHeader";import{SiteFooter}from"@/components/SiteFooter";import{JsonLd,breadcrumbJson}from"@/lib/seo";
export const metadata:Metadata={title:"AI Model Routing Methodology — Rules, Budgets and Fallbacks",description:"Learn how to design transparent model-routing policies using ordered rules, cost guards, evaluation evidence, failure isolation, and observable decision traces.",alternates:{canonical:"/router/methodology"},openGraph:{url:"/router/methodology",title:"AXON//RADAR Model Routing Methodology",description:"A practical method for explainable, resilient AI model routing.",images:[]},twitter:{images:[]}};
const sections=[{n:"01",title:"Route from evidence",body:"Use representative evaluation results, operational constraints, and real workload data. A router should encode a decision already supported by evidence—not hide an unsupported preference."},{n:"02",title:"Keep rules inspectable",body:"Rules should be ordered, narrow, and readable. Task type, request size, explicit keywords, budget, and required capability are understandable signals; opaque routing should be treated as a separate learned system."},{n:"03",title:"Prefer the smallest sufficient model",body:"A cheaper route is valuable only when it satisfies the task. Establish a quality floor with evaluations, then use cost and latency to choose among models that clear it."},{n:"04",title:"Fail deliberately",body:"Fallbacks are for execution failures and capacity problems, not silent quality repair. Keep the chain short, avoid circular routes, apply timeouts, and record every attempt."},{n:"05",title:"Guard before execution",body:"Estimate cost before dispatch and enforce a hard maximum. Actual cost can differ because providers tokenize and bill differently, so monitor real usage as well as estimates."},{n:"06",title:"Observe and revise",body:"Record the selected rule, model, latency, tokens, cost, fallback attempts, and outcome. Re-run evaluation suites when models, prices, prompts, or provider behavior change."}];
export default function RouterMethodology(){return <><SiteHeader/><main className="router-method"><JsonLd data={breadcrumbJson([{name:"Home",path:"/"},{name:"Router",path:"/router"},{name:"Methodology",path:"/router/methodology"}])}/><section className="router-method-hero"><span>ROUTING DISCIPLINE / 01</span><h1>Every route<br/><em>needs a reason.</em></h1><p>An effective router is a visible operational policy: it explains what matched, why a model was selected, what it may cost, and what happens when it fails.</p><Link href="/router">OPEN ROUTER WORKSPACE ↗</Link></section><section className="router-method-grid">{sections.map(section=><article key={section.n}><span>{section.n}</span><h2>{section.title}</h2><p>{section.body}</p></article>)}</section><section className="router-method-check"><div><span>PRODUCTION CHECKLIST</span><h2>Before a policy goes live.</h2></div><ul><li>Representative evaluation evidence exists for every routed task class.</li><li>Every rule has an owner, reason, order, and bounded match condition.</li><li>Cost ceilings, timeouts, provider limits, and maximum output are enforced.</li><li>Fallbacks use compatible capabilities and never form circular chains.</li><li>Decision traces exclude secrets and sensitive prompt content.</li><li>Routing quality, spend, latency, and failure rate are reviewed continuously.</li></ul></section></main><SiteFooter/></>}
