"use client";import Link from"next/link";import{useMemo,useState}from"react";import{benchmarkModels,metricInfo,type MetricKey}from"@/lib/benchmarks";
export function BenchmarkExplorer({initialMetric="intelligence" as MetricKey}){const[metric,setMetric]=useState<MetricKey>(initialMetric),[access,setAccess]=useState("All"),[query,setQuery]=useState("");const rows=useMemo(()=>benchmarkModels.filter(m=>(access==="All"||m.access===access)&&`${m.name} ${m.provider}`.toLowerCase().includes(query.toLowerCase())&&m.scores[metric]!==undefined).sort((a,b)=>(b.scores[metric]??0)-(a.scores[metric]??0)),[metric,access,query]);const best=rows[0]?.scores[metric]??1;return <div className="benchmark-explorer"><div className="benchmark-controls"><div className="metric-tabs">{(Object.keys(metricInfo)as MetricKey[]).map(key=><button type="button" key={key} className={metric===key?"active":""} onClick={()=>setMetric(key)}>{metricInfo[key].short}</button>)}</div><div className="benchmark-filter"><select aria-label="Filter by access" value={access} onChange={e=>setAccess(e.target.value)}><option>All</option><option>Proprietary</option><option>Open weights</option></select><input aria-label="Search models" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search model or lab"/></div></div><div className="metric-definition"><div><span>ACTIVE MEASURE</span><h2>{metricInfo[metric].name}</h2></div><p>{metricInfo[metric].description} <Link href="/benchmarks/methodology">Methodology ↗</Link></p></div><div className="benchmark-table"><div className="benchmark-row benchmark-head"><span>RANK</span><span>MODEL</span><span>ACCESS</span><span>SCORE</span><span>RELATIVE</span><span/></div>{rows.map((m,i)=>{const score=m.scores[metric]??0;return <Link className="benchmark-row" href={`/benchmarks/models/${m.slug}`} key={m.slug}><span className="bench-rank">{String(i+1).padStart(2,"0")}</span><span className="bench-model"><b>{m.name}</b><small>{m.provider}</small></span><span><em className={m.access==="Open weights"?"open":""}>{m.access}</em></span><span className="bench-score"><b>{score}</b><small>{metricInfo[metric].unit}</small></span><span className="bench-bar"><i style={{width:`${Math.max(4,score/best*100)}%`}}/></span><span>↗</span></Link>})}</div><p className="benchmark-caveat">Ranks apply only to the selected evaluation and cited snapshot. Missing scores are not estimated. Harness, reasoning effort, provider, and version can materially change results.</p></div>}
