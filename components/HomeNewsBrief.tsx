"use client";
import{useEffect,useState}from"react";
type Story={id:string;title:string;summary:string;source:string;url:string;publishedAt:string;category:string};
export function HomeNewsBrief(){const[stories,setStories]=useState<Story[]>([]);const[state,setState]=useState<"loading"|"ready"|"error">("loading");useEffect(()=>{fetch("/api/news",{cache:"no-store"}).then(response=>response.ok?response.json():Promise.reject()).then(data=>{setStories((data.items??[]).slice(0,5));setState("ready")}).catch(()=>setState("error"))},[]);if(state==="loading")return <div className="home-news-loading"><i/><span>CONNECTING TO LIVE PUBLISHERS</span></div>;if(state==="error"||!stories.length)return <div className="home-news-loading error"><span>LIVE FEED TEMPORARILY UNAVAILABLE</span><a href="/news">OPEN NEWSROOM ↗</a></div>;const[lead,...rest]=stories;return <div className="home-news-grid"><a className="home-news-lead" href={lead.url} target="_blank" rel="noreferrer"><div><span>{lead.category}</span><time>{new Date(lead.publishedAt).toLocaleDateString("en",{month:"short",day:"numeric",year:"numeric"})}</time></div><h3>{lead.title}</h3><p>{lead.summary}</p><footer><b>{lead.source}</b><span>READ ORIGINAL ↗</span></footer></a><div className="home-news-side">{rest.map(story=><a href={story.url} target="_blank" rel="noreferrer" key={story.id}><div><span>{story.category}</span><time>{new Date(story.publishedAt).toLocaleDateString("en",{month:"short",day:"numeric"})}</time></div><h3>{story.title}</h3><footer><b>{story.source}</b><span>↗</span></footer></a>)}</div></div>}
