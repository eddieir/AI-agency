import type {Tool} from "@/lib/catalog";
export function ToolCard({tool,index=0}:Readonly<{tool:Tool;index?:number}>){return <article className="tool-card"><div className="tool-card-top"><span className="tool-index">{String(index+1).padStart(2,"0")}</span><span className={`pill pill-${tool.price.toLowerCase().replace(" ","-")}`}>{tool.price}</span></div><div className="tool-identity"><span className="tool-mark">{tool.mark}</span><div><small>{tool.company}</small><h3>{tool.name}</h3></div></div><p>{tool.summary}</p><div className="tag-row">{tool.bestFor.map(tag=><span key={tag}>{tag}</span>)}</div><a href={tool.url} target="_blank" rel="noreferrer" aria-label={`Visit ${tool.name}`}>View tool <span>↗</span></a></article>}
