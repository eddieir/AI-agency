export function PageHero({code,title,italic,description,tone="dark",children}:Readonly<{code:string;title:string;italic:string;description:string;tone?:"dark"|"lime"|"coral";children?:React.ReactNode}>){return <section className={`page-hero tone-${tone}`}><div><span className="section-code">{code}</span><h1>{title}<br/><em>{italic}</em></h1></div><div className="page-hero-side"><p>{description}</p>{children}</div></section>}
